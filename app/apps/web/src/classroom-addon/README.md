# Classroom add-on

Iframe routes Classroom loads. Query params and `getAddOnContext` are specified in `docs/classroom.md`.

Do not ship a student view that can start a lesson without `courseId` and `itemId`.
