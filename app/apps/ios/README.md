# iOS

Native SwiftUI. Same major features as the companion web app. PencilKit, native audio, push. No Live Activities.

Open from a Classroom item (`courseId`, `itemId`). Keep Xcode work on the Mac.
